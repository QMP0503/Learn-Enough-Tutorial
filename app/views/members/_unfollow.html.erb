<%= form_with(model: current_member.active_relationships.find_by(followed_id: @member.id),
             html: { method: :delete }, local: false) do |f| %>
  <%= f.submit "Unfollow", class: "btn" %>
<% end %>
