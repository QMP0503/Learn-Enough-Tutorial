<%= form_with(model: @quote, local: true) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="mb-3">
    <%= f.text_area :content, placeholder: "Share your thoughts...", class: "form-control", rows: 3 %>
  </div>
  <div class="mb-3">
    <span class="image">
      <%= f.file_field :image, class: "form-control" %>
    </span>
  </div>
  <div class="d-grid">
    <%= f.submit "Share Quote", class: "btn btn-primary" %>
  </div>
<% end %>
